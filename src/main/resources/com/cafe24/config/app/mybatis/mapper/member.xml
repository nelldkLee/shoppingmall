<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	
	<insert id="insert" parameterType="membervo">
		INSERT INTO member(id, member_name, email, telephone, gender, zipcode, normal_address, extend_address) 
			VALUES(#{id}, #{memberName}, #{email}, #{telephone}, #{gender}, #{zipcode}, #{normalAddress}, #{extendAddress}) 
	</insert>
	
	<select id="findByEmail" resultType="membervo" parameterType="string">
		SELECT 
			member_no, id, member_name, email, telephone, gender, birth, regdate, zipcode, normal_address, extend_address 
		FROM member WHERE id = #{id}
	</select>
	
</mapper>
